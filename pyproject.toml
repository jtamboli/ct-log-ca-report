[project]
name = "ct-log-ca-report"
version = "0.1.0"
description = "Certificate Transparency Log CA Report Tool"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27.0",
    "cryptography>=42.0.0",
]

[tool.hatch.build.targets.wheel]
packages = ["."]
